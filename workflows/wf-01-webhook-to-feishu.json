{
  "name": "01 万能通知：任何事件 → 飞书消息/卡片",
  "nodes": [
    {
      "parameters": { "httpMethod": "POST", "path": "notify", "responseMode": "lastNode", "options": {} },
      "id": "hook", "name": "🌐 接收事件 Webhook", "type": "n8n-nodes-base.webhook", "typeVersion": 2,
      "position": [-500, 300], "webhookId": "universal-notify"
    },
    {
      "parameters": {
        "resource": "messaging", "operation": "sendCardMessage",
        "receiveIdType": "open_id", "receiveId": "← 替换为你的 open_id",
        "cardTitle": "=📢 {{ $json.title || '新通知' }}",
        "cardContent": "={{ $json.body || JSON.stringify($json, null, 2) }}",
        "cardColor": "={{ $json.severity === 'critical' ? 'red' : $json.severity === 'warning' ? 'yellow' : 'blue' }}"
      },
      "id": "feishu", "name": "→ 飞书卡片", "type": "@lixiongwei/n8n-nodes-feishu.feishu", "typeVersion": 2,
      "position": [-250, 300],
      "credentials": { "feishuApi": { "id": "PLACEHOLDER", "name": "飞书 Feishu account" } }
    }
  ],
  "connections": { "🌐 接收事件 Webhook": { "main": [[{ "node": "→ 飞书卡片", "type": "main", "index": 0 }]] } },
  "pinData": {}, "versionId": "1.0.0", "active": false, "settings": {},
  "tags": [{ "name": "通知" }, { "name": "Webhook" }, { "name": "CI/CD" }]
}
